{
  "name": "youtube-premium-plus",
  "version": "1.0.0",
  "description": "YouTube Premium Plus Extension",
  "scripts": {
    "build": "vite build && vite build --config vite.config.external.js",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^5.0.0",
    "vitest": "^4.1.8"
  },
  "dependencies": {
    "animejs": "^4.4.1"
  }
}
